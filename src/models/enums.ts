export enum UserGoal {
    WEIGHT_LOSS = "Weight loss",
    MUSCLE_GAIN = "Muscle gain"
};

export enum Gender {
    MALE = "Male",
    FEMALE = "Female"
};

export enum UserActivityLevel {
    ACTIVE_SEDENTARY = "Little to no exercise and desk job",
    ACTIVE_LIGHT = "Light exercise(1-3 days/week) and desk job",
    ACTIVE_MODERATE = "Hard exercise(3-5 days/week) and desk job",
    ACTIVE_INTENSE = "Light exercise(3-5 days/week) and active job",
    ACTIVE_EXTREME = "Hard exercise(3-5 days/week) and active job"
};
